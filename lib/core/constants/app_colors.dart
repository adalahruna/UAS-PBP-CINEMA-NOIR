import 'package:flutter/material.dart';

class AppColors {
  // --- WARNA UTAMA ---

  /// Warna Latar Belakang utama (Hitam pekat/gelap)
  static const Color darkBackground = Color(0xFF121212);

  /// Warna Aksen/Primer (Emas Elegan)
  static const Color gold = Color(0xFFD4AF37);

  // --- WARNA SEKUNDER ---

  /// Warna putih untuk teks
  static const Color textWhite = Color(0xFFFFFFFF);

  /// Warna abu-abu untuk teks sekunder/hint
  static const Color textGrey = Color(0xFF8A8A8A);

  /// Warna container yang sedikit lebih terang dari background
  static const Color darkGrey = Color(0xFF1E1E1E);

  // --- UNTUK COMMUNITY FEATURE (UPDATED TO GOLD THEME) ---
  
  /// Warna primer untuk Community - gunakan gold
  static const Color primaryPurple = Color(0xFFD4AF37); // Changed to gold
  
  /// Warna putih standar
  static const Color white = Color(0xFFFFFFFF);
  
  /// Warna abu-abu standar
  static const Color grey = Color(0xFF8A8A8A);
}
